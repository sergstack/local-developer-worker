# Telemetry

Telemetry stores only tool name, version, counts, latency, status, review outcome, fallback and hashes. It must not store source code, log bodies, secrets, prompts or provider responses.
